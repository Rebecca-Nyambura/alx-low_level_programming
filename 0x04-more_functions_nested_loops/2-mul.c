#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two integers
 * @a: integer to multiply with b
 * @b: integer to multiply with a
 * Return: multiplication result
 */
int mul(int a, int b)
{
	return (a * b);
}
